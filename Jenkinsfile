/**
* @param value  the name of the library
* @param changelog  if checked, changes to the library will trigger new builds to run
*/
@Library(value='jenkins-templates', changelog=false) _  // Use underscore as placeholder to include everything
jenkinsfilePy()  // Invokes call() method
